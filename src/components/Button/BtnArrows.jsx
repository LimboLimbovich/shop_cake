export default function BtnArrows ({children}) {
  return (
    <button className='border border-[#FED84C] rounded-full bg-[#FED84C] w-16 h-16 flex items-center justify-center text-4xl'>
    {children}
  </button>
  )
}